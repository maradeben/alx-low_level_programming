#include <stdio.h>

/**
 * main - short program
 * Description:print lowercase alphabet
 * Return: returns 0 when run sucessfully
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
