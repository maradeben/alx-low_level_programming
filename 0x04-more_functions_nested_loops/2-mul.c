#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies ints
 * @x: integer
 * @y: integer
 * Description: compute the product of two ints
 * Return: sum
 */
int mul(int x, int y)
{
	return (x * y);
}
